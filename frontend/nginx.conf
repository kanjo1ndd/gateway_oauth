server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /profile {
        proxy_pass http://gateway:8080/profile;
    }

    location /oauth2 {
        proxy_pass http://gateway:8080/oauth2;
    }
}